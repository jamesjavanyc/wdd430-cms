import Contact from "@app/contacts/contact.model"

export default class Message{
    constructor(
        id: string,
        subject: string,
        msgText: string,
        sender: Contact
    ) {
        
    }
}